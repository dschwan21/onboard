import { PageShell } from "@/components/layout/page-shell";

export default function LibraryPage() {
  return (
    <PageShell
      title="Library"
      description="Pro-only resource archive placeholder for prompts, templates, and recordings."
    />
  );
}
